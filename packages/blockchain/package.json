{
  "name": "@paychain/blockchain",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\" --max-warnings 0",
    "test": "echo \"no tests in @paychain/blockchain\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
